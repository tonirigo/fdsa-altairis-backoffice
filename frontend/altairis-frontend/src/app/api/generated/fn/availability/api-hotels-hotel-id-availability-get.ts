/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HotelInventoryGridDto } from '../../models/hotel-inventory-grid-dto';

export interface ApiHotelsHotelIdAvailabilityGet$Params {
  hotelId: number;
  from?: string;
  to?: string;
}

export function apiHotelsHotelIdAvailabilityGet(http: HttpClient, rootUrl: string, params: ApiHotelsHotelIdAvailabilityGet$Params, context?: HttpContext): Observable<StrictHttpResponse<HotelInventoryGridDto>> {
  const rb = new RequestBuilder(rootUrl, apiHotelsHotelIdAvailabilityGet.PATH, 'get');
  if (params) {
    rb.path('hotelId', params.hotelId, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HotelInventoryGridDto>;
    })
  );
}

apiHotelsHotelIdAvailabilityGet.PATH = '/api/hotels/{hotelId}/availability';
