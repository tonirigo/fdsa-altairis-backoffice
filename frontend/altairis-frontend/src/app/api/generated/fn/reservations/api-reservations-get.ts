/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReservationDtoPagedResult } from '../../models/reservation-dto-paged-result';

export interface ApiReservationsGet$Params {
  hotelId?: number;
  from?: string;
  to?: string;
  status?: 'Pending' | 'Confirmed' | 'Cancelled';
  page?: number;
  pageSize?: number;
}

export function apiReservationsGet(http: HttpClient, rootUrl: string, params?: ApiReservationsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<ReservationDtoPagedResult>> {
  const rb = new RequestBuilder(rootUrl, apiReservationsGet.PATH, 'get');
  if (params) {
    rb.query('hotelId', params.hotelId, {});
    rb.query('from', params.from, {});
    rb.query('to', params.to, {});
    rb.query('status', params.status, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReservationDtoPagedResult>;
    })
  );
}

apiReservationsGet.PATH = '/api/Reservations';
