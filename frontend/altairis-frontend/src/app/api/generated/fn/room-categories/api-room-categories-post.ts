/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateRoomCategoryDto } from '../../models/create-room-category-dto';
import { RoomCategoryDto } from '../../models/room-category-dto';

export interface ApiRoomCategoriesPost$Params {
      body?: CreateRoomCategoryDto
}

export function apiRoomCategoriesPost(http: HttpClient, rootUrl: string, params?: ApiRoomCategoriesPost$Params, context?: HttpContext): Observable<StrictHttpResponse<RoomCategoryDto>> {
  const rb = new RequestBuilder(rootUrl, apiRoomCategoriesPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RoomCategoryDto>;
    })
  );
}

apiRoomCategoriesPost.PATH = '/api/room-categories';
