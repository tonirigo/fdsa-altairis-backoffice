/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateHotelDto } from '../../models/create-hotel-dto';
import { HotelDto } from '../../models/hotel-dto';

export interface ApiHotelsPost$Params {
      body?: CreateHotelDto
}

export function apiHotelsPost(http: HttpClient, rootUrl: string, params?: ApiHotelsPost$Params, context?: HttpContext): Observable<StrictHttpResponse<HotelDto>> {
  const rb = new RequestBuilder(rootUrl, apiHotelsPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HotelDto>;
    })
  );
}

apiHotelsPost.PATH = '/api/Hotels';
