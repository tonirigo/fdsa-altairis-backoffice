/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HotelDto } from '../../models/hotel-dto';

export interface ApiHotelsIdGet$Params {
  id: number;
}

export function apiHotelsIdGet(http: HttpClient, rootUrl: string, params: ApiHotelsIdGet$Params, context?: HttpContext): Observable<StrictHttpResponse<HotelDto>> {
  const rb = new RequestBuilder(rootUrl, apiHotelsIdGet.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HotelDto>;
    })
  );
}

apiHotelsIdGet.PATH = '/api/Hotels/{id}';
