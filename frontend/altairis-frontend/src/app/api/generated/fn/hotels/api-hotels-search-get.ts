/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HotelDtoPagedResult } from '../../models/hotel-dto-paged-result';

export interface ApiHotelsSearchGet$Params {
  query?: string;
  page?: number;
  pageSize?: number;
}

export function apiHotelsSearchGet(http: HttpClient, rootUrl: string, params?: ApiHotelsSearchGet$Params, context?: HttpContext): Observable<StrictHttpResponse<HotelDtoPagedResult>> {
  const rb = new RequestBuilder(rootUrl, apiHotelsSearchGet.PATH, 'get');
  if (params) {
    rb.query('query', params.query, {});
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HotelDtoPagedResult>;
    })
  );
}

apiHotelsSearchGet.PATH = '/api/Hotels/search';
